import ReactComponentExample from '../components/ReactComponentExample';
import { createEntry } from '../helpers/entries';

createEntry({
    name: 'ReactComponentPage',
    component: ReactComponentExample,
});
